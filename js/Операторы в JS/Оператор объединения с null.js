//Оператор объединения с null (??)

/*
Оператор ?? возвращает первый аргумент,
если он не null / undefined, иначе второй.
*/

let name; //Если задать значение = Vladislav, то
console.log(name ?? "Без имени"); //получим Vladislav, а не Без имени
